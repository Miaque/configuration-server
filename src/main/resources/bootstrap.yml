server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Miaque/configuration-server
          search-paths: configurationserver, organizationservice
          username: Miaque
          password: 99bbseseb


